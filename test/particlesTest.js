const assert = require('assert')

describe('particles', () => {
    it('should start empty', () => {
        let particles = []
        assert.strictEqual(particles.length, 0)
    })

    it('should be create from hitSplash', () => {
        hitSpl
    })
})